# Core dependencies for Bandit's Advantage v3
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
pyarrow>=14.0          # Preferred for Parquet (History Archive); falls back to CSV if unavailable
pytest>=7.0

# Environment variable support (.env loading)
python-dotenv>=1.0
cryptography>=42.0  # For v0.3 lightweight encryption in persistence layer (Fernet)
duckdb>=0.10  # Recommended query engine for SOT (Parquet-native analytical queries, Excel/Power BI friendly)
